server:
  port: 8079
       
spring.cloud.stream.bindings:
  input:
    destination: guest
    group: guest
    consumer:
      partitioned: false
      instanceCount: 2
      instanceIndex: 1
        
auth:
  username: admin
  password: admin  
  client:
    id: Guest
    secret: GuestSecret
  server:
    token:
      url: http://localhost:8500/uaa/oauth/token